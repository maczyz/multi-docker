language: generic
sudo: required
services:
  - docker

before_install:
#  at the ned we dom't use . but ./client --> the ast parameter is a context, so our context is client directory  
  - docker build -t mateusz/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true mateusz/react-test npm test

after_success:
  - docker build -t mateusz/multi-client ./client
  - docker build -t mateusz/multi-nginx ./nginx
  - docker build -t mateusz/multi-server ./server
  - docker build -t mateusz/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push mateusz/multi-client
  - docker push mateusz/multi-nginx
  - docker push mateusz/multi-server
  - docker push mateusz/multi-worker

deploy:
  provider: elasticbeanstalk
  region: us-west-1
  app: multi-docker
  env: MultiDocker-env
  bucket_name: elasticbeanstalk-us-west-1-306476627547
  bucket_path: docker-multi
  on:
    branch: master
  access_key_id: $AWS_ACCESS_KEY
  secret_access_key:
    secure: $AWS_SECRET_KEY
